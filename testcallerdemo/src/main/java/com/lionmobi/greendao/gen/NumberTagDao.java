package com.lionmobi.greendao.gen;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.lionmobi.greendao.entry.PhoneNumber;
import com.lionmobi.greendao.entry.TagInfo;

import com.lionmobi.greendao.entry.NumberTag;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "NUMBER_TAG".
*/
public class NumberTagDao extends AbstractDao<NumberTag, Long> {

    public static final String TABLENAME = "NUMBER_TAG";

    /**
     * Properties of entity NumberTag.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property _id = new Property(0, Long.class, "_id", true, "_id");
        public final static Property Count = new Property(1, Integer.class, "count", false, "COUNT");
        public final static Property Update_time = new Property(2, long.class, "update_time", false, "UPDATE_TIME");
    };

    private DaoSession daoSession;


    public NumberTagDao(DaoConfig config) {
        super(config);
    }
    
    public NumberTagDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"NUMBER_TAG\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: _id
                "\"COUNT\" INTEGER," + // 1: count
                "\"UPDATE_TIME\" INTEGER NOT NULL );"); // 2: update_time
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"NUMBER_TAG\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, NumberTag entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        Integer count = entity.getCount();
        if (count != null) {
            stmt.bindLong(2, count);
        }
        stmt.bindLong(3, entity.getUpdate_time());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, NumberTag entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        Integer count = entity.getCount();
        if (count != null) {
            stmt.bindLong(2, count);
        }
        stmt.bindLong(3, entity.getUpdate_time());
    }

    @Override
    protected final void attachEntity(NumberTag entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public NumberTag readEntity(Cursor cursor, int offset) {
        NumberTag entity = new NumberTag( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // _id
            cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1), // count
            cursor.getLong(offset + 2) // update_time
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, NumberTag entity, int offset) {
        entity.set_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setCount(cursor.isNull(offset + 1) ? null : cursor.getInt(offset + 1));
        entity.setUpdate_time(cursor.getLong(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(NumberTag entity, long rowId) {
        entity.set_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(NumberTag entity) {
        if(entity != null) {
            return entity.get_id();
        } else {
            return null;
        }
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getTagInfoDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getPhoneNumberDao().getAllColumns());
            builder.append(" FROM NUMBER_TAG T");
            builder.append(" LEFT JOIN TAG_INFO T0 ON T.\"_id\"=T0.\"_id\"");
            builder.append(" LEFT JOIN PHONE_NUMBER T1 ON T.\"_id\"=T1.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected NumberTag loadCurrentDeep(Cursor cursor, boolean lock) {
        NumberTag entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        TagInfo tagInfo = loadCurrentOther(daoSession.getTagInfoDao(), cursor, offset);
        entity.setTagInfo(tagInfo);
        offset += daoSession.getTagInfoDao().getAllColumns().length;

        PhoneNumber phoneNumber = loadCurrentOther(daoSession.getPhoneNumberDao(), cursor, offset);
        entity.setPhoneNumber(phoneNumber);

        return entity;    
    }

    public NumberTag loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<NumberTag> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<NumberTag> list = new ArrayList<NumberTag>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<NumberTag> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<NumberTag> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
